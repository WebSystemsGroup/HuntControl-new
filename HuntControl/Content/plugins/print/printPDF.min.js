function printPDF(b64) {bf(b64, 'application/pdf');}function bf(b64Data, ct){ct = ct || '';var sliceSize = 512;b64Data = b64Data.replace(/^[^,]+,/, '');b64Data = b64Data.replace(/\s/g, '');var byteCharacters = window.atob(b64Data);var bas = [];for (var offset = 0; offset < byteCharacters.length; offset += sliceSize) {var slice = byteCharacters.slice(offset, offset + sliceSize);var bn = new Array(slice.length);for (var i = 0; i < slice.length; i++) {bn[i] = slice.charCodeAt(i);}var ba = new Uint8Array(bn);bas.push(ba);}var b = new Blob(bas, { type: ct });var b_url = URL.createObjectURL(b);printJS({ printable: b_url });return b;}