@{
    ViewBag.Title = "BirdsV2";
}

<iframe width="100%"  height='40em' src="@Model></iframe>

<div id="BirdsPrint">asdsadasd</div>

@*<script>
        var iframe = $('<iframe id="iframe1">');
        iframe.css('width', '100%');
        iframe.css('height', 'calc(100vh - 300px)');
        iframe.attr("src", "data:application/pdf;base64," + '@Model');
        $('#BirdsPrint').html(iframe);-->



        $.ajax({
            url: '/Parser/GetCovidSertificate',
            data: { customer_id: '36f8b20f-af56-42e5-9c25-11962a55e4ab' },
            type: 'POST',
            beforeSend: function () {
                $.blockUI(LoaderContent);
            },
            success: function (data) {
                response = data._response;
                response_byte = data._file
                var iframe = $('<iframe id="iframe1">');
                iframe.css('width', '100%');
                iframe.css('height', 'calc(100vh - 300px)');
                iframe.attr("src", "data:application/pdf;base64," + response_byte);
                $('#IframeResult').html(iframe);
            },
            complete: function () {
                $.unblockUI();
            }
        })
    </script>*@



